#pragma once

#define SETTINGS_BACKGROUND 0 // 1 or 2 will use a couple background, 0 leaves it blank.
                              //    it's recommended to leave the window multiplier set to 1 when
                              //    first trying the backgrounds
#define SETTINGS_WINDOW_MULTIPLIER 1 // must be positive integer